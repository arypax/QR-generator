PORT=3000
# Local dev only. On Netlify/Vercel either leave BASE_URL empty or set it to the exact production domain.
# If BASE_URL is wrong (for example http://localhost:3000 in production), Google OAuth and QR links will break.
BASE_URL=http://localhost:3000
SESSION_SECRET=change_me
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
# Google OAuth redirect URI must include:
# https://YOUR_DOMAIN/auth/google/callback
DATABASE_URL=postgres://user:pass@host:5432/dbname
